ZSH_DIR="~/.config/zsh"

# vars
source $ZSH_DIR/vars

# oh-my-zsh config
source $ZSH_DIR/ohmyzsh

# aliases
source $ZSH_DIR/aliases

# everything else
source $ZSH_DIR/misc

# include local rc file
if [[ -f ~/.zshrc.local ]]; then
	source ~/.zshrc.local
fi

# starship
eval "$(starship init zsh)"
